{
  "name": "@powerline/embed",
  "version": "2.0.4.0",
  "private": true,
  "type": "module",
  "description": "Powerline embeddable call-campaign widget",
  "scripts": {
    "build": "vite build && vite build --config vite.webrtc.config.ts",
    "dev": "vite build --config vite.webrtc.config.ts && vite build --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@twilio/voice-sdk": "^2.11.0"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
